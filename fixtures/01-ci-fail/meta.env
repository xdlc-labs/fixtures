BRANCH=fixture/01-ci-fail
TITLE=fix: include high byte in fold checksum
BODY=The health payload checksum was dropping the high byte of the running sum, so two-byte sequences that overflowed 255 compared equal. Fold now keeps the full 16-bit width.
